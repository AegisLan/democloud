server:
  port: 9100

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AegisLan/cloud-config-repo.git
          search-paths: /**
          username: AegisLan
          password: hem2535440
      label: master


